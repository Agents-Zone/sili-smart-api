# 清空与审计 实现计划

> **目标：** 管理员清空与独占开关切换保存时正向缓存、反向占用索引、最近绑定记录三批同清；满载降级审计标记进入消费日志 admin_info；缓存统计接口扩展独占/复用/降级三指标；README 补多实例内存模式部署边界说明。
> **架构：** 清空扩展全部落在 `service/channel_affinity.go` 现有 Clear 系列函数与 `model/option.go` 的配置更新后处理；降级审计沿用现有 ginKeyChannelAffinityLogInfo → AppendChannelAffinityAdminInfo 链路追加 exclusive_degrade 子结构；统计扩展 GetChannelAffinityCacheStats 遍历反向占用索引；README 为纯文档变更。
> **技术栈：** Go 1.22+、Gin、testify（require/assert）

---

## 文件结构

### 新建文件
- `service/channel_affinity_clear_test.go` — 清空三批同清、开关联动清空、统计口径、降级审计的测试

### 修改文件
- `service/channel_affinity.go` — ClearChannelAffinityCacheAll/ClearChannelAffinityCacheByRuleName 三批同清；GetChannelAffinityCacheStats 扩展三指标；GetChannelAffinityExclusiveStats 导出统计 helper；降级计数器读取
- `service/channel_affinity_exclusive.go` — 降级计数器变量与原子读函数、按规则清空索引的辅助（按正向键回放）
- `model/option.go` — handleConfigUpdate 后处理 channel_affinity_setting.rules 的 exclusive_bind 变化联动清空
- `controller/channel_affinity_cache.go` — 无签名变更（响应体经 stats 结构自然扩展，03 文档 3.2）
- `README.md` 及 5 个语言版本 — 部署章节补多实例内存模式边界（T13）

---

## 数据模型来源

| 来源 | 文件路径 | 状态 |
|------|----------|------|
| 04_model_interface.md | `/context/05_specs/P2_CHL_001_FEAT_渠道独占绑定/04_model_interface.md` | ✅ 存在 |

**引用说明：** 无 DDL 变更，计划不含数据库任务。降级审计标记结构引用：04_model_interface.md → §4.3 exclusive_degrade 标记（rule_name/key_fp/channel_id/channel_binding_count）。清空范围引用：04_model_interface.md → §6.2 同批清理一致性。

**03 接口文档内部契约对应关系：** 降级审计标记 → T8（MarkChannelAffinityUsed 扩展）；警告日志 → T8（acquireExclusiveBinding 降级分支 LogWarn）；降级计数 → T4（计数器）+ T8（GetChannelAffinityDegradedReuseTotal）。

---

## 任务清单

- [ ] **T6: 管理员清空三批同清**

  **文件：**
  - 修改：`service/channel_affinity.go:198-246`（ClearChannelAffinityCacheAll、ClearChannelAffinityCacheByRuleName）
  - 修改：`service/channel_affinity_exclusive.go`（追加 clear 辅助）
  - 测试：`service/channel_affinity_clear_test.go`

  **契约：**
  - exclusive 文件追加签名：
    - `func clearExclusiveRuntimeAll()`：occupancy 与 lastBind 两命名空间整体清空（各自 cache.Keys() 后 DeleteMany，或 Purge）；失败重试一次，仍失败 SysError（SSOT 5.2.5）
    - `func clearExclusiveRuntimeByForwardKeys(forwardKeys []string)`：按正向缓存键回放清理。取向为成员级移除（与 04 文档 §6.2 三命名空间同批清空语义对齐，避免误删其它规则同渠道的键指纹）：对每个 forwardKey（含命名空间前缀的 full key）读正向缓存值得 channelID；键指纹由正向键推导——正向键后缀含 affinity 值原文（buildChannelAffinityCacheKeySuffix 构成：rule/model/group/value 依次拼接），对 value 段调 affinityFingerprint 得指纹（多个候选段时逐一试 occupancyRemoveKeyFP，命中即止）；随后 lastBindDelete（去正向前缀）。推导失败的键（无法定位指纹）跳过并 SysLog 声明，残留依赖 TTL 收敛，实现时在函数注释声明此边界
    - `func retryOnce(fn func() error) error`：执行失败重试一次（供清空使用，SSOT 5.2.5）。固定保留为独立函数：clearExclusiveRuntimeAll 与 clearExclusiveRuntimeByForwardKeys 两处调用，表达清空路径的稳定重试语义

  - ClearChannelAffinityCacheAll 扩展：现有正向清空后调 `clearExclusiveRuntimeAll()`；返回值语义不变（正向删除条数，03 文档 3.3）
  - ClearChannelAffinityCacheByRuleName 扩展：现有 DeleteByPrefix 前先收集该前缀下全部键（cache.Keys 过滤前缀），删除正向后调 `clearExclusiveRuntimeByForwardKeys(收集的键)`；校验逻辑（未知规则名、未启用 include_rule_name 报错）不变
  - 关键逻辑：清空失败不影响正向清空结果返回（响应 success 不变，失败仅 SysError，03 文档 3.3 错误表）
  - 依赖：T2（occupancy/lastBind 存储）；import 无新增

  **验收锚点：**
  - 核心断言：`TestClearChannelAffinityCacheAllClearsExclusive`：先造数（正向 SetWithTTL 两键、occupancyAddKeyFP 两渠道、lastBindSet 两键），调 ClearChannelAffinityCacheAll() 后断言：occupancy 两渠道 `occupancyBindingCount` 均返回键数 0、lastBind 两键 found 均为 false、正向 Get found 均为 false。`TestClearChannelAffinityCacheByRuleNameClearsExclusive`：造数含规则 "ruleA"（include_rule_name=true）与 "ruleB" 两前缀，调 ClearChannelAffinityCacheByRuleName("ruleA") 后断言 ruleA 前缀三处全清、ruleB 前缀正向与索引仍在（返回值 deleted 等于 ruleA 正向条数）。`TestClearByRuleKeepsOtherRuleMemberOnSharedChannel`：ruleA 与 ruleB 的键均登记到同一渠道 801（满载复用形态），按 ruleA 清空后断言 occupancyBindingCount(801) 仍返回键数 1 且仅含 ruleB 键的指纹（成员级移除，其它规则同渠道成员不误删，04 文档 §6.2）
  - 验证命令：`go test ./service/ -run 'TestClearChannelAffinityCache' -v`
  - 预期输出：`ok github.com/QuantumNous/new-api/service`，全部子测试 PASS

  **specs 依据：**
  - 章节范围：§5.2.2（处理流程第4条）、§5.2.4（业务规则规则2）、§5.2.5（异常处理）、§4.2.3（功能与按钮：清空操作）
  - 业务规则索引：
    - [BR1] §5.2.4 规则2：清空操作必须正向、反向、最近绑定三批同批执行
    - [BR2] §5.2.5 异常「索引清空失败」：重试一次仍失败记错误，管理端反馈沿用现有
    - [BR3] §4.2.3 功能说明：清空按钮行为见 5.2 反向索引管理（三命名空间联动）

  **依赖：** T2（子计划 01 的存储层）

  **提交信息：** feat(affinity): 管理员全部清空与按规则清空扩展到反向索引与最近绑定记录

- [ ] **T7: 独占开关切换保存联动清空**

  **文件：**
  - 修改：`model/option.go:606-642`（handleConfigUpdate 后处理）
  - 修改：`service/channel_affinity_exclusive.go`（追加规则级清空入口）
  - 测试：`service/channel_affinity_clear_test.go`

  **契约：**
  - exclusive 文件追加签名：`func HandleChannelAffinityRulesUpdate(oldRulesJSON string, newRulesJSON string)`：common.Unmarshal 两侧规则数组（失败返回），按 name 对齐逐规则比对 ExclusiveBind；对发生变化的规则调用按规则清空（复用 ClearChannelAffinityCacheByRuleName 的清空路径，但跳过其 include_rule_name 校验——开关切换清空对未启用 include_rule_name 的规则同样生效，按旧规则定义的键构成回放清除：include_rule_name 开启时按规则名前缀 DeleteByPrefix；未开启时按旧规则键构成（IncludeModelName/IncludeUsingGroup 组合 + affinity 值）无法回放出可区分前缀的，才退化为正向缓存整体清空 + clearExclusiveRuntimeAll，并在 SysLog 声明该退化，03 文档 3.1 服务端处理逻辑第2条）；清空失败 SysError 不中断（保存以落库为准，03 文档 3.1）
  - handleConfigUpdate 接入：`configName == "channel_affinity_setting" && configKey == "rules"` 分支中，在 `UpdateConfigFromMap` 覆盖前读取旧值（从 config.GlobalConfig 导出或从 common.OptionMap 读取旧串），随后正常更新配置并以新 value 调 `service.HandleChannelAffinityRulesUpdate(旧, 新)`。model 包调用 service 包会形成 import 环（service 依赖 model），因此该后处理改为在 `model/option.go` 的 handleConfigUpdate 内不直接调用 service，而是通过既有的解耦方式：参照 billing_setting 后处理在 model 包内调 InvalidatePricingCache（model 自身函数）的模式，把联动入口挂到 `operation_setting` 包暴露钩子——在 `setting/operation_setting/channel_affinity_setting.go` 增加包级回调变量 `OnRulesExclusiveBindChanged func(oldJSON, newJSON string)`（默认 nil），handleConfigUpdate 的 channel_affinity_setting 分支调用它；`service` 包 init（`service/channel_affinity_exclusive.go` 的 init()）注册实现 `operation_setting.OnRulesExclusiveBindChanged = HandleChannelAffinityRulesUpdate`。此接线保持 model→operation_setting 单向、service→operation_setting 单向，无环
  - 关键逻辑：旧值获取在 UpdateConfigFromMap 覆盖前读取（handleConfigUpdate 内先 `old := cfg 反射导出 rules 当前值` 或经 config.GlobalConfig.Get 后断言类型取 Rules 序列化）；JSON 解析失败静默返回（保存主流程不受影响）
  - 依赖：T1、T6；import `setting/config` 已有、`common`

  **验收锚点：**
  - 核心断言：`TestHandleChannelAffinityRulesUpdate`（表驱动，3 例，service 包）：
    - 例1（开关变化）：旧 `[{"name":"r1","exclusive_bind":false}]` 新 `[{"name":"r1","exclusive_bind":true}]`，预先对规则 r1 造数（正向键 "r1:fp1"、occupancy、lastBind），调用后断言 r1 三处全清
    - 例2（开关未变）：旧新 exclusive_bind 均 true，调用后造数仍在（无清空）
    - 例3（非法 JSON）：旧串非 JSON 数组，调用无 panic、无清空（造数仍在）
  - 验证命令：`go test ./service/ -run TestHandleChannelAffinityRulesUpdate -v`
  - 预期输出：`ok github.com/QuantumNous/new-api/service`，3 个子测试 PASS

  **specs 依据：**
  - 章节范围：§4.1.4（业务规则规则1）、§5.2.2（处理流程第4条）
  - 业务规则索引：
    - [BR1] §4.1.4 规则1：开关状态变化并保存成功时，同步清空该规则正向缓存与反向占用索引
    - [BR2] §4.1.4 规则1 约束：开启独占不残留旧共享绑定，关闭立即回软亲和
    - [BR3] §5.2.2 流程第4条：开关切换保存时三命名空间同批清空

  **依赖：** T6

  **提交信息：** feat(affinity): exclusive_bind 开关切换保存联动清空该规则三命名空间运行时数据

- [ ] **T8: 降级审计标记与警告日志**

  **文件：**
  - 修改：`service/channel_affinity.go:676-711`（MarkChannelAffinityUsed、AppendChannelAffinityAdminInfo）
  - 修改：`service/channel_affinity_exclusive.go`（降级计数器读取函数）
  - 测试：`service/channel_affinity_clear_test.go`

  **契约：**
  - 签名：`func GetChannelAffinityDegradedReuseTotal() uint64`（`atomic.LoadUint64(&channelAffinityDegradedReuseTotal)`，计数器变量在 T4 已建于 exclusive 文件）
  - MarkChannelAffinityUsed 扩展：info map 追加键 `exclusive_degrade`，值为 acquireExclusiveBinding 在 T4 写入 gin context 的降级标记（`ginKeyChannelAffinityExclusiveDegrade` 常量，类型 map[string]interface{}，含 rule_name、key_fp、channel_id、channel_binding_count 四字段，结构引用：04_model_interface.md → §4.3）；无降级时不追加该键。AppendChannelAffinityAdminInfo 无需改动（info map 整体挂 admin_info["channel_affinity"]，exclusive_degrade 嵌于其下，非管理员视图剥离 admin_info 沿用现有机制）
  - 警告日志：acquireExclusiveBinding 降级分支在 T4 计数处同步 `logger.LogWarn(c, fmt.Sprintf(...))`，要素含规则名、键指纹、渠道 ID、当时绑定键数（若 T4 未落 logger 依赖，本任务补上；与请求关联经 c 传递）
  - 关键逻辑：审计写入失败静默跳过（map 写入无失败路径，仅日志失败场景）；降级审计不改变请求响应与计费（SSOT 5.3.4 规则1）；进程内降级计数器经 GetChannelAffinityDegradedReuseTotal 由 T9 读入缓存统计接口响应 degraded_reuse_total 字段（03 文档 3.2、SSOT 5.3.2 第3条）
  - 依赖：T4；import `logger`（若 T4 未引入）

  **验收锚点：**
  - 核心断言：`TestMarkChannelAffinityUsedWithDegrade`：gin context 设 meta 与 `ginKeyChannelAffinityExclusiveDegrade` 标记 map（rule_name="r1", key_fp="fp123456", channel_id=9, channel_binding_count=3），调 MarkChannelAffinityUsed(ctx, "default", 9) 后经 `AppendChannelAffinityAdminInfo(ctx, adminInfo)` 断言 `adminInfo["channel_affinity"].(map[string]interface{})["exclusive_degrade"]` 深等于输入 map（四字段齐全）。`TestMarkChannelAffinityUsedWithoutDegrade`：context 无降级标记时 info map 中无 exclusive_degrade 键。`TestDegradedCounter`：两次模拟降级计数（直接 atomic.Add 或经测试钩子）后 `GetChannelAffinityDegradedReuseTotal()` 返回 2
  - 验证命令：`go test ./service/ -run 'TestMarkChannelAffinityUsed|TestDegradedCounter' -v`
  - 预期输出：`ok github.com/QuantumNous/new-api/service`，全部 PASS

  **specs 依据：**
  - 章节范围：§5.3.2（处理流程第1、2条）、§5.3.3（涉及数据）、§5.3.4（业务规则）、§5.3.5（异常处理）
  - 业务规则索引：
    - [BR1] §5.3.2 流程第1条：降级时 admin_info 追加规则名、键指纹、渠道 ID、当时绑定键数
    - [BR2] §5.3.2 流程第2条：同步输出与请求关联的 LogWarn
    - [BR3] §5.3.4 规则1：审计不改变请求正常响应与计费
    - [BR4] §5.3.5 异常：审计写入失败静默跳过，不影响请求
    - [BR5] §5.3.3 数据项：审计仅存指纹，非管理员视图剥离 admin_info（沿用现有机制）

  **依赖：** T4、T5

  **提交信息：** feat(affinity): 满载降级审计标记进入消费日志 admin_info 与关联警告日志

- [ ] **T9: 缓存统计接口扩展三指标**

  **文件：**
  - 修改：`service/channel_affinity.go:72-79`（ChannelAffinityCacheStats）、`service/channel_affinity.go:111-196`（GetChannelAffinityCacheStats）
  - 测试：`service/channel_affinity_clear_test.go`

  **契约：**
  - 结构扩展：`ChannelAffinityCacheStats` 追加三字段（json tag 与 03 文档 3.2 一致）：`ExclusiveBindings int \`json:"exclusive_bindings"\``、`SharedBindings int \`json:"shared_bindings"\``、`DegradedReuseTotal uint64 \`json:"degraded_reuse_total"\``
  - 统计 helper（exclusive 文件）：`func GetChannelAffinityExclusiveStats() (exclusive int, shared int)`：occupancyCache.Keys() 遍历全部渠道条目（每键 Get 后 len(KeyFPs)），键数 1 的渠道计入 exclusive（贡献 1 键）、大于 1 计入 shared（贡献其键数）；键遍历失败返回 (0, 0) 且 SysError（03 文档 3.2 边界值：失败返回 0）
  - GetChannelAffinityCacheStats 扩展：setting==nil 的早退分支三字段零值；正常分支末尾调 GetChannelAffinityExclusiveStats 与 GetChannelAffinityDegradedReuseTotal 填充
  - 关键逻辑：口径为渠道维度全局统计、跨规则（SSOT 4.2.4 规则1）；成员残留滞后窗口属设计内（04 文档 §4.1），不做成员级精确过期；响应向后兼容仅新增字段（03 文档 5.3.4 规则2）
  - 依赖：T2、T8；import 无新增

  **验收锚点：**
  - 核心断言：`TestGetChannelAffinityCacheStatsExclusiveMetrics`：occupancy 造数渠道 A 键数 1、渠道 B 键数 3，计数器置 5 后调 GetChannelAffinityCacheStats()，断言 `stats.ExclusiveBindings == 1`（A 贡献 1）、`stats.SharedBindings == 3`（B 贡献 3）、`stats.DegradedReuseTotal == 5`；且原有字段 Total/ByRuleName 值与造数一致（无回归）。`TestGetChannelAffinityExclusiveStats_Empty`：无任何条目时返回 (0, 0)
  - 验证命令：`go test ./service/ -run 'TestGetChannelAffinityCacheStats|TestGetChannelAffinityExclusiveStats' -v`
  - 预期输出：`ok github.com/QuantumNous/new-api/service`，全部 PASS

  **specs 依据：**
  - 章节范围：§4.2.2（显示字段）、§4.2.4（业务规则规则1）、§4.2.5（交互逻辑）、§5.3.2（处理流程第3条）
  - 业务规则索引：
    - [BR1] §4.2.4 规则1：按反向占用索引实时统计，键数 1 记独占、大于 1 记复用，不区分规则全局口径
    - [BR2] §4.2.2 字段「累计降级次数」：进程启动累计，重启清零
    - [BR3] §5.3.4 规则2：统计接口向后兼容扩展，仅新增字段
    - [BR4] §4.2.5 交互：空值或统计不可用按通用规范显示 -（前端职责，后端保证失败返回 0）

  **依赖：** T8

  **提交信息：** feat(affinity): 缓存统计接口扩展独占绑定数/复用绑定数/累计降级次数三指标

- [ ] **T13: README 部署章节补多实例内存模式边界说明**

  **文件：**
  - 修改：`README.md`、`README.zh_CN.md`、`README.en.md`、`README.zh_TW.md`、`README.ja.md`、`README.fr.md`（部署/环境变量相关章节，各语言版本同步）

  **契约：**
  - 内容：渠道独占绑定依赖运行时共享缓存。多实例部署且未启用 Redis（内存缓存模式）时，反向占用索引与原子占位在实例间不可见，独占判定与先到先得语义退化为现有软亲和（SSOT 5.2.2 第6条）。部署章节以一句边界说明 + 指向 Redis 配置（REDIS_CONN_STRING）的方式呈现，语言风格与各 README 现有部署说明一致
  - 关键逻辑：纯文档变更，无代码；不新增运行时检测（SSOT 5.2.2 第6条明确不增加）

  **验收锚点：**
  - 核心断言：6 个 README 文件部署/缓存章节各含一处独占绑定多实例边界说明；按各语言实际新增内容的关键词逐一 grep（README.md 与 README.en.md 内容相同的英文版用 "exclusive bind"，README.zh_CN.md 用 "独占"，README.zh_TW.md 用 "獨佔"，README.ja.md 用 "排他"，README.fr.md 用 "liaison exclusive"）均各命中 1 次。注意：不得以 "REDIS_CONN_STRING" 或裸 "exclusive" 作为断言词（改动前即命中，无法区分新增内容；README.fr.md 第 59 行已有 "exclusivement"）
  - 验证命令：`grep -ci "exclusive bind" README.md && grep -c "独占" README.zh_CN.md && grep -c "獨佔" README.zh_TW.md && grep -c "排他" README.ja.md && grep -ci "liaison exclusive" README.fr.md && grep -ci "exclusive bind" README.en.md`
  - 预期输出：`1`（六次输出各为 1）

  **specs 依据：**
  - 章节范围：§5.2.2（处理流程第6条：多实例部署边界）
  - 业务规则索引：
    - [BR1] §5.2.2 第6条：多实例 + 内存模式的独占退化经部署文档说明，不增加运行时检测

  **依赖：** 无（文档任务，可与其它任务并行；建议随 T7 同批提交）

  **提交信息：** docs: README 补充渠道独占绑定多实例内存模式部署边界
