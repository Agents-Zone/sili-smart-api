# 前端配置观测 实现计划

> **目标：** 规则编辑弹窗新增独占绑定开关（含开启前重点提醒），设置区统计行展示独占/复用/降级三指标，新增文案补全 7 种语言。
> **架构：** 开关作为 rule-editor-dialog 表单字段随现有保存链路提交（types.ts 的 AffinityRule 增 exclusive_bind，序列化自然带入 rules JSON）；三指标消费设置区已对接的 getCacheStats（/api/option/channel_affinity_cache），展示在现有 Cache Entries 统计行旁（用户已确认展示位置；usage-logs 页的单键详情弹窗 CacheStatsDialog 按接口文档 3.4 保持不改）。文案走 i18next 英文源字符串为键。
> **技术栈：** React 19、TypeScript、Base UI（SettingsSwitchField/Alert）、i18next、Bun

---

## 文件结构

### 新建文件
- 无

### 修改文件
- `web/src/features/system-settings/general/channel-affinity/types.ts` — AffinityRule 增 exclusive_bind、CacheStats 增三指标字段
- `web/src/features/system-settings/general/channel-affinity/rule-editor-dialog.tsx` — 独占绑定开关字段、开启时高亮提醒
- `web/src/features/system-settings/general/channel-affinity/constants.ts` — RULE_TEMPLATES 两模板补 exclusive_bind: false 显式字段
- `web/src/features/system-settings/general/channel-affinity/index.tsx` — 操作栏统计行追加三指标展示
- `web/src/i18n/locales/en.json`、`zh.json`、`zh-TW.json`、`fr.json`、`ru.json`、`ja.json`、`vi.json` — 新增文案键（7 语言）

---

## 数据模型来源

| 来源 | 文件路径 | 状态 |
|------|----------|------|
| 04_model_interface.md | `/context/05_specs/P2_CHL_001_FEAT_渠道独占绑定/04_model_interface.md` | ✅ 存在 |

**引用说明：** 无 DDL 变更，计划不含数据库任务。前端字段与后端 JSON 契约引用：04_model_interface.md → §3.1 exclusive_bind 字段说明；指标字段引用：接口文档 03_api_interface.md → 3.2 新增响应字段说明。

**SSOT 偏离记录回引（specs §8.3，代码评审期补记、计划编写后回填）：** D5 键指纹为 SHA1 前 16 位 hex（提醒文案与统计展示涉及指纹口径时以 specs 8.3 为准）。偏离以 specs 8.3 登记为准。

---

## 任务清单

- [x] **T10: 类型定义与规则编辑弹窗独占开关**

  **文件：**
  - 修改：`web/src/features/system-settings/general/channel-affinity/types.ts:25-39`（AffinityRule）、`web/src/features/system-settings/general/channel-affinity/constants.ts:88-117`（RULE_TEMPLATES）
  - 修改：`web/src/features/system-settings/general/channel-affinity/rule-editor-dialog.tsx:75-87`（RuleFormValues）、`rule-editor-dialog.tsx:123-137`（defaultValues）、`rule-editor-dialog.tsx:142-165`（resetFromRule）、`rule-editor-dialog.tsx:193-245`（handleSave）、`rule-editor-dialog.tsx:302-306` 附近（开关渲染区）
  - 测试：`web/src/features/system-settings/general/channel-affinity/rule-editor-dialog.test.tsx`（由实现者按 TDD 指导创建；若项目无组件测试基建，则以类型检查 + 构建为验收，见验证命令）

  **契约：**
  - 类型签名：`AffinityRule` 追加 `exclusive_bind?: boolean`（可选，旧 JSON 缺字段兼容）；`CacheStats` 追加 `exclusive_bindings?: number`、`shared_bindings?: number`、`degraded_reuse_total?: number`（均可选，接口失败或旧后端时容错）；`RULE_TEMPLATES` 的 codexCli/claudeCli 模板各追加 `exclusive_bind: false`
  - 组件契约：`RuleFormValues` 追加 `exclusive_bind: boolean`；defaultValues 置 false；resetFromRule 读 `r.exclusive_bind ?? false`；handleSave 组装 rule 对象时带 `exclusive_bind: values.exclusive_bind`
  - 渲染：在 skip_retry_on_failure 开关（现有 SettingsSwitchField）之后追加一个 `SettingsSwitchField`，label 用 `t('Exclusive Bind')`，description 用 `t('When enabled, a channel is exclusively bound to a single affinity key; when all available channels are occupied, the channel with the fewest bindings is reused')`；开关下方条件渲染高亮提醒（复用现有 `Alert` + `AlertDescription`，destructive 变体或现有警告样式类），仅 `form.watch('exclusive_bind') === true` 时展示，内容两点合一为一段文案键 `t("Exclusive bind granularity follows this rule's affinity key. With token_id it equals per api_key exclusivity; with other key types one api_key may occupy multiple or all channels. When all available channels are occupied, reuse applies and exclusivity is suspended")`（双引号包裹避免撇号转义，键与 T12 清单逐字一致）
  - 关键逻辑：提醒随开关状态实时显隐（form.watch 响应式），关闭即隐藏，无二次确认（SSOT 4.1.5）；开关值随现有 Save 按钮提交，无新增按钮（SSOT 4.1.3）；visual 与 JSON 双模式互通（serializeRules/parseRules 走对象展开，exclusive_bind 自然携带）
  - 依赖：`react-hook-form`（form.register/watch）、`react-i18next`、现有组件（SettingsSwitchField、Alert）

  **验收锚点：**
  - 核心断言：`rule-editor-dialog.test.tsx`（若建，置于 `web/src/features/system-settings/general/channel-affinity/__tests__/`，按项目现有组件测试模式：node:test + happy-dom 手写 harness，参考 `src/features/keys/components/__tests__/api-key-group-cell.test.tsx`）：render RuleEditorDialog（open=true, rule 含 exclusive_bind: true）后断言提醒文本可见（getByText 匹配提醒文案键）；传 exclusive_bind: false 或缺省时提醒不存在（queryByText 为 null）；模拟切换开关后提醒显隐切换。若不建组件测试，则以 `bun run typecheck` 类型断言 exclusive_bind 字段存在于 AffinityRule 与 RuleFormValues
  - 验证命令：`cd web && bun run typecheck`（必跑）；组件测试存在时另跑 `node --test src/features/system-settings/general/channel-affinity/__tests__/rule-editor-dialog.test.tsx`
  - 预期输出：typecheck 无错误输出（exit 0）；组件测试全 PASS

  **specs 依据：**
  - 章节范围：§4.1.2（涉及字段）、§4.1.3（功能与按钮）、§4.1.4（业务规则规则2）、§4.1.5（交互逻辑）
  - 业务规则索引：
    - [BR1] §4.1.2 字段校验：exclusive_bind 布尔必填，默认关闭
    - [BR2] §4.1.4 规则2：文案走 i18next 英文键，补全部 7 语言
    - [BR3] §4.1.5 交互：开启时展示粒度与满载两点高亮提醒，随开关实时显隐，关闭不展示，无需二次确认
    - [BR4] §4.1.3 功能说明：无新增按钮，开关随现有保存提交，loading 校验沿用现状

  **依赖：** 无（可与子计划 01/02 并行开发，但执行顺序保持在后；类型契约独立自洽）

  **提交信息：** feat(web): 亲和规则编辑弹窗新增独占绑定开关与开启前重点提醒

- [x] **T11: 设置区统计行三指标展示**

  **文件：**
  - 修改：`web/src/features/system-settings/general/channel-affinity/index.tsx:539-544`（SettingsPageActionsPortal 内统计行）
  - 测试：无独立测试文件（展示逻辑为纯渲染，由 T10 的类型契约与构建覆盖）

  **契约：**
  - 渲染契约：现有 `cacheStats &&` 统计行从单一 Cache Entries 扩展为四个数值并排：`{t('Cache Entries')}: {cacheStats.total} / {cacheStats.cache_capacity}`（现状保留）后追加 `· {t('Exclusive Bindings')}: {formatStat(cacheStats.exclusive_bindings)}`、`· {t('Shared Bindings')}: {formatStat(cacheStats.shared_bindings)}`、`· {t('Degraded Reuse Total')}: {formatStat(cacheStats.degraded_reuse_total)}`
  - helper：`const formatStat = (v: number | undefined): string => (v === undefined || v === null ? '-' : String(v))`（模块级函数或内联于统计行上方；空值显示 -，SSOT 4.2.5）
  - 关键逻辑：数据源为现有 refreshCache → getCacheStats（打开/刷新设置页时已加载，无新请求）；字段可选容错（旧后端或索引失败返回缺省时显示 -）；只读展示无按钮（SSOT 4.2.3）
  - 依赖：T10（CacheStats 类型已扩展）；无新增 import

  **验收锚点：**
  - 核心断言：无自动化断言（纯展示），以类型契约与构建验证；手动验收锚点：设置页渠道亲和性卡片操作栏显示四个指标，后端返回 exclusive_bindings=42 时显示 42，字段缺失时显示 -
  - 验证命令：`cd web && bun run typecheck && bun run build`
  - 预期输出：typecheck 无错误；build 输出 `built in` 成功（exit 0）

  **specs 依据：**
  - 章节范围：§4.2.2（显示字段）、§4.2.4（业务规则规则1 的展示侧）、§4.2.5（交互逻辑）、§4.2.3（功能与按钮）
  - 业务规则索引：
    - [BR1] §4.2.5 交互：三个指标只读数值展示，空值或统计不可用显示 -
    - [BR2] §4.2.2 字段「独占绑定数」：当前处于独占状态的绑定键数
    - [BR3] §4.2.2 字段「复用绑定数」：满载复用共享状态绑定键数
    - [BR4] §4.2.2 字段「累计降级次数」：进程启动累计，重启清零

  **依赖：** T10

  **提交信息：** feat(web): 渠道亲和性设置区统计行展示独占/复用/降级三指标

- [x] **T12: i18n 七语言文案补全**

  **文件：**
  - 修改：`web/src/i18n/locales/en.json`、`web/src/i18n/locales/zh.json`、`web/src/i18n/locales/zh-TW.json`、`web/src/i18n/locales/fr.json`、`web/src/i18n/locales/ru.json`、`web/src/i18n/locales/ja.json`、`web/src/i18n/locales/vi.json`
  - 测试：以 i18n 同步工具校验代替独立测试

  **契约：**
  - 新增键清单（英文源字符串为键，T10/T11 中出现的全部 t() 新键）：
    - `Exclusive Bind`
    - `When enabled, a channel is exclusively bound to a single affinity key; when all available channels are occupied, the channel with the fewest bindings is reused`
    - `Exclusive bind granularity follows this rule's affinity key. With token_id it equals per api_key exclusivity; with other key types one api_key may occupy multiple or all channels. When all available channels are occupied, reuse applies and exclusivity is suspended`
    - `Exclusive Bindings`
    - `Shared Bindings`
    - `Degraded Reuse Total`
  - 各语言值要求：en 键值同键名；zh 为简体中文准确翻译（提醒文案须完整表达粒度边界与满载降级两点，对齐 SSOT 4.1.5）；zh-TW 繁化；fr/ru/ja/vi 各自语言翻译；不使用机器直译腔，管理端术语与现有 locale 中亲和性条目风格一致（参考现有 "Channel Affinity"、"Switch affinity on success" 等条目译法）
  - 关键逻辑：键入 translation 对象，扁平结构与现有文件一致；JSON 键序按字母序插入现有排序位置（保持与 `bun run i18n:sync` 工具约定一致）
  - 依赖：T10、T11（键清单来源）

  **验收锚点：**
  - 核心断言：`bun run i18n:sync` 校验输出无 missing key 报告；7 个文件 grep 每键各命中 1 次（键与值同行）
  - 验证命令：`cd web && bun run i18n:sync && grep -c "Exclusive Bindings" src/i18n/locales/*.json`
  - 预期输出：i18n:sync 无缺失告警；grep 每个 locale 文件输出 `1`（zh.json 含翻译值行仍计 1 次键命中）

  **specs 依据：**
  - 章节范围：§4.1.4（业务规则规则2）、§7.2（内部功能依赖：前端 i18n 体系）
  - 业务规则索引：
    - [BR1] §4.1.4 规则2：标签与说明文案走 i18next 英文键，补 7 种语言
    - [BR2] §7.2 依赖「前端开关与指标」：新增文案补充全部 7 种语言 locale 文件

  **依赖：** T10、T11

  **提交信息：** feat(web/i18n): 渠道独占绑定开关与指标文案补全 7 种语言
