# 对话内容记录 实现计划

> **目标：** 在 relay 中转链路上以最小侵入方式捕获请求与响应明文，按会话聚合入库 ClickHouse 日志库，向管理员提供完整多轮对话查询能力。
> **架构：** 新增一个挂于 relay 路由分组的 gin middleware 与一套查询 API。middleware 同步段只捕获纯值（请求/响应字节 + context 标量），`gopool.Go` 异步闭包内完成协议解析、启发式会话识别、轮次写库；查询侧按 `created_at, request_id` 升序拼接完整对话。仅改动 `router/relay-router.go` 与 `router/api-router.go` 各一处，不碰任何中转 handler、relay 流程与 RelayInfo。
> **技术栈：** Go 1.22+、Gin、GORM v2、ClickHouse 日志库（`LOG_SQL_DSN`）、go-redis（会话缓存）、gopool（异步编排）、go-i18n（错误信息）。

---

## 文件结构

### 新建文件
- `model/conversation.go` — 轮次模型与 ClickHouse 建表/写库/聚合查询
- `service/conversation.go` — 类型定义、三协议消息解析、用量解析、启发式会话识别、写库编排、对话拼接
- `middleware/conversation_log.go` — 对话捕获 gin middleware 与响应体捕获器
- `controller/conversation.go` — 会话列表与详情管理查询接口

### 修改文件
- `router/relay-router.go` — `httpRouter` 与 `relayGeminiRouter` 各挂载一次 `middleware.ConversationLog()`（Distribute 之前）
- `router/api-router.go` — 注册 `GET /api/conversation/` 与 `GET /api/conversation/:session_key`（管理员路由组）
- `i18n/keys.go` — 新增会话不存在错误 Msg 常量
- `i18n/locales/en.yaml`、`i18n/locales/zh-CN.yaml`、`i18n/locales/zh-TW.yaml` — 新增对应翻译键

### 测试文件（由实现者按 TDD 指导创建）
- `model/conversation_test.go` — 建表 DDL 生成、显示 id 回填（不依赖真实 ClickHouse）
- `service/conversation_test.go` — 解析、识别、拼接核心逻辑
- `middleware/conversation_log_test.go` — 开关关闭、白名单过滤、响应截断

---

## 数据模型来源

| 来源 | 文件路径 | 状态 |
|------|----------|------|
| 04_model_interface.md | `/context/05_specs/P1_TECH_001_TECH_对话内容记录/04_model_interface.md` | ✅ 存在 |

**引用说明：** `conversation_turns` 表 DDL 以 04_model_interface.md 为唯一维护点。本组件存储仅 ClickHouse 日志库（不落三主库），建表采用 lazy 建表（`sync.Once` 保护，仿 `migrateClickHouseLogDB`），非独立迁移文件方式。T1 中建表 DDL 引用 04_model_interface.md，计划内不复制完整 SQL。

---

## 任务清单

- [x] **T1: model 层轮次模型与 ClickHouse 持久化**

  **文件：**
  - 创建：`model/conversation.go`
  - 引用：`/context/05_specs/P1_TECH_001_TECH_对话内容记录/04_model_interface.md` → `conversation_turns` 建表语句
  - 测试：`model/conversation_test.go`（由实现者按 TDD 指导创建）

  **契约：**
  - 类型定义：
    ```go
    package model

    // ConversationTurn 一行一轮的会话轮次记录，仅落 ClickHouse 日志库（LOG_SQL_DSN）。
    // Messages 存 []MsgPart 的 JSON 序列化（common.Marshal 写入），查询侧解码。
    type ConversationTurn struct {
        Id                int64  `json:"id"`
        SessionKey        string `json:"session_key"`
        RequestId         string `json:"request_id"`
        CreatedAt         int64  `json:"created_at"`
        Messages          string `json:"messages"`
        TurnKind          string `json:"turn_kind"`
        Truncated         int64  `json:"truncated"`
        ModelName         string `json:"model_name"`
        ChannelId         int    `json:"channel_id"`
        TokenId           int    `json:"token_id"`
        TokenName         string `json:"token_name"`
        UserId            int    `json:"user_id"`
        Username          string `json:"username"`
        Group             string `json:"group"`
        Ip                string `json:"ip"`
        IsStream          bool   `json:"is_stream"`
        UseTime           int    `json:"use_time"`
        UpstreamRequestId string `json:"upstream_request_id"`
        PromptTokens      int    `json:"prompt_tokens"`
        CompletionTokens  int    `json:"completion_tokens"`
    }

    // ConversationSummary 会话列表聚合行，按 session_key 聚合，不含消息内容。
    type ConversationSummary struct {
        SessionKey    string `json:"session_key"`
        FirstTurnTime int64  `json:"first_turn_time"`
        LastTurnTime  int64  `json:"last_turn_time"`
        TurnCount     int64  `json:"turn_count"`
        TokenName     string `json:"token_name"`
        Username      string `json:"username"`
        UserID        int64  `json:"user_id"`
        ModelName     string `json:"model_name"`
    }

    // ConversationQueryParams 会话列表过滤维度，对齐 logs 查询参数。
    type ConversationQueryParams struct {
        TokenName      string
        Username       string
        ModelName      string
        StartTimestamp int64
        EndTimestamp   int64
    }

    var conversationEnsureOnce sync.Once

    func EnsureConversationTable()
    func conversationTurnTTLDays() int
    func conversationTurnCreateTableSQL(ttlDays int) string
    func assignTurnIds(turns []ConversationTurn, startIdx int)
    func RecordConversationTurn(turn *ConversationTurn) error
    func ListConversations(params *ConversationQueryParams, page, pageSize int) ([]ConversationSummary, int64, error)
    func GetConversationTurns(sessionKey string) ([]ConversationTurn, error)
    ```
  - 关键逻辑：
    - `EnsureConversationTable()`：`sync.Once` 保护，仅当 `common.LogDatabaseType() == common.DatabaseTypeClickHouse` 时经 `LOG_DB.Exec(conversationTurnCreateTableSQL(...))` 建表；TTL 天数据 `conversationTurnTTLDays()`（复用 `LOG_SQL_CLICKHOUSE_TTL_DAYS`，仿 `clickHouseLogTTLDays`，负值归 0）。
    - `conversationTurnCreateTableSQL(ttlDays)`：仿 `clickHouseLogCreateTableSQL`（model/main.go:437），DDL 结构引用 04_model_interface.md 的 `conversation_turns` 建表语句：`ENGINE = MergeTree()`、`PARTITION BY toYYYYMM(toDateTime(created_at))`、`ORDER BY (session_key, created_at, request_id)`、TTL 复用 model 包内 `clickHouseLogTTLExpression/clickHouseLogTTLClause`；`group` 列名用 `logGroupCol` 方言变量包裹（ClickHouse 反引号）。
    - `RecordConversationTurn(turn)`：`LOG_DB.Create(turn)`；失败经 `common.SysError` 记录（写库失败不应返回给调用方影响请求）。
    - `ListConversations`：`LOG_DB.Table("conversation_turns")` 按 `session_key` 聚合，`GROUP BY session_key`，每会话一行。维度列 `token_name/username/user_id/model_name` 用 `any()` 聚合函数包裹（ClickHouse 跨库聚合约束），首末轮时间用 `min/max(created_at)`，轮数 `count(*)`。过滤维度 `token_name/username/model_name` 精确匹配，`start_timestamp/end_timestamp` 过滤 `created_at`。按末轮时间倒序，`Limit(pageSize).Offset((page-1)*pageSize)`。总数用子查询 `SELECT count() FROM (SELECT session_key FROM conversation_turns WHERE ... GROUP BY session_key)` 经 `LOG_DB.Raw` 执行返回。返回值比 specs §2.4 能力5 补充 `total`（分页必需，controller 的 PageInfo 需要）。
    - `GetConversationTurns(sessionKey)`：`LOG_DB.Table("conversation_turns").Where("session_key = ?", sessionKey).Order("created_at asc, request_id asc")` 取全部行；返回的 `Messages` 为原始 JSON 字符串，解码在 service 层（保持 model 不依赖 service 类型）。
    - `assignTurnIds`：仿 `assignDisplayLogIds`（model/log.go:110），从 `startIdx` 起按序回填展示用 `Id`（ClickHouse 无自增）。签名取值切片 `turns []ConversationTurn`，元素级写入经共享底层数组生效；`GetConversationTurns` 查询侧全量按序回填（`assignTurnIds(turns, 0)`，从 1 起）。

  **验收锚点：**
  - 核心断言：
    - `conversationTurnCreateTableSQL(30)` 生成的 SQL 同时包含 `CREATE TABLE IF NOT EXISTS conversation_turns`、`ENGINE = MergeTree()`、`PARTITION BY toYYYYMM(toDateTime(created_at))`、`ORDER BY (session_key, created_at, request_id)`、`TTL toDateTime(created_at) + INTERVAL 30 DAY DELETE`；TTL 传 0 时不含 `TTL ` 片段。
    - `assignTurnIds`：3 个空轮次从 startIdx=0 回填 Id 得 `[1,2,3]`；startIdx=20 得 `[21,22,23]`；nil 输入不 panic。
    - `conversationTurnTTLDays()`：环境变量 `LOG_SQL_CLICKHOUSE_TTL_DAYS=30` 时返回 30；负数返回 0。
  - 验证命令：`cd e:/Agent-zone/sili-smart-trace && go test ./model/ -run 'TestConversationTurnCreateTableSQL|TestAssignTurnIds|TestConversationTurnTTLDays' -v`
  - 预期输出：所有用例 `--- PASS`，测试命令退出码 0。

  **specs 依据：**
  - 章节范围：§2.3（输出定义-数据库表）、§2.4 能力5（会话持久化）、§2.4 能力6（会话查询 model 部分）、§3.1 硬约束（存储仅 ClickHouse）、§3.3 兼容性要求（数据库兼容性、`group` 保留字）、§3.5 可靠性要求（建表幂等、失败请求入库）、§4.2 引入方式
  - 业务规则索引：
    - [BR1] §2.4 能力5：建表幂等，`sync.Once` 保护，仅 ClickHouse 走手写 DDL
    - [BR2] §2.4 能力5：`group` 保留字列名按 `logGroupCol` 方言约定处理（ClickHouse 反引号）
    - [BR3] §2.4 能力5：SELECT 中 `session_key` 之外维度列必须 `any()` 聚合包裹，禁止裸返回非聚合列；首末轮时间与轮数是时间窗口内轮次统计
    - [BR4] §3.5 可靠性：失败/错误响应同样入库，缺失维度记零值
    - [BR5] §2.4 能力6：轮次顺序由查询侧 `ORDER BY created_at, request_id` 决定，写入路径不维护 `turn_seq`
    - [BR6] §3.1 硬约束：`conversation_turns` 仅落 ClickHouse 日志库，不落 SQLite/MySQL/PostgreSQL

  **依赖：** 无

  **提交信息：** feat(conversation): 新增轮次模型与 ClickHouse 持久化层（由控制器统一提交）

---

- [x] **T2: service 层协议消息解析与用量解析**

  **文件：**
  - 创建：`service/conversation.go`
  - 测试：`service/conversation_test.go`（由实现者按 TDD 指导创建）

  **契约：**
  - 类型与函数签名：
    ```go
    package service

    // MsgPart 消息片段，kind 区分 text/tool_use/tool_result。
    type MsgPart struct {
        Role string `json:"role"` // user / assistant / tool / system
        Kind string `json:"kind"` // text / tool_use / tool_result
        Text string `json:"text"`
    }

    // parseRequestMessages 按 path 定协议，把请求体 messages 归一化成 []MsgPart。
    func parseRequestMessages(path string, body []byte) ([]MsgPart, error)

    // parseAssistantContent 输出同结构 []MsgPart：text 段标 Kind=text，
    // function_call/tool_use 标 Kind=tool_use。非流式解析 JSON（common.Unmarshal），
    // 流式逐行扫描 SSE（仿 relay/helper/stream_scanner.go）。
    func parseAssistantContent(path string, isStream bool, respBytes []byte) []MsgPart

    // parseUsage 从响应字节解析 prompt/completion tokens，本表 token 唯一来源。
    func parseUsage(path string, isStream bool, respBytes []byte) (promptTokens, completionTokens int)
    ```
  - 关键逻辑：
    - `parseRequestMessages`：按 path 分支定协议。OpenAI（`/v1/chat/completions`、`/v1/completions`）解析 `messages[]`，`role=tool` 标 `Kind=tool_result`；OpenAI Responses 路径（`/v1/responses`、`/v1/responses/compact`）经 `parseOpenAIResponsesInput` 解析 `input[]` 而非 `messages`（真实 API 无 messages 字段）：input 为 string 记 user text，`[]any` 元素 string 记 user text、map 取 role 与 content 文本，role=tool 标 `Kind=tool_result`；Claude（`/v1/messages`）解析 `messages[]`，`user` role 的 `content[]` 中 `type=tool_result` block 标 `Kind=tool_result`；Gemini（后缀 `:generateContent`/`:streamGenerateContent`）解析 `contents[]`。使用 `common.Unmarshal`，禁止直接调用 `encoding/json`。解析失败返回 error。
    - `parseAssistantContent`：非流式按 path 解析 JSON；流式逐行扫描 SSE（跳过 `event:` 行、剥 `data:` 前缀、跳过 `[DONE]`）。按协议分派增量位置：OpenAI chat/completions 载荷无 type，text 取 `choices[].delta.content/reasoning_content`，工具取 `delta.tool_calls[].function.name/arguments`（text 存工具名与入参紧凑表示，如 `get_weather({"city":"北京"})`）；OpenAI Responses 按事件类型，text 取 `response.output_text.delta`，工具取 `response.output_item.added(function_call)` 与 `response.function_call_arguments.delta`；Claude 按 `content_block_delta` 分派，工具参数增量在 `delta.type="input_json_delta"` 的 `partial_json`；Gemini 解析 `candidates[].content.parts[]` 与 `functionCall`。错误响应（上游 4xx/5xx JSON）无消息体时记空 `[]MsgPart`。
    - `parseUsage`：OpenAI 非流式取顶层 `usage`；流式取末个带 `usage` 的 chunk（无则记 0）；Responses 流式取 `response.completed` 事件的 `Response.Usage`；Claude 非流式取顶层 `usage`，流式合并 `message_start` 与 `message_delta` 两处 `usage.input_tokens/output_tokens`；Gemini 取 `usageMetadata`，流式取末个带 `usageMetadata` 的 chunk。只用响应字节，不做计费运算。
  - 协议路径常量：抽为包级常量（`openAIProtocol`、`claudeProtocol`、`geminiProtocol`）或复用 path 分支 helper，保证 T3/T4 复用同一协议判定逻辑。

  **验收锚点：**
  - 核心断言（表驱动，场景明确、预期值具体）：
    - `parseRequestMessages("/v1/chat/completions", body含role=tool消息)` → 该元素 `Kind == "tool_result"`、`Role == "tool"`
    - `parseRequestMessages("/v1/messages", body含user的content中tool_result block)` → 该元素 `Kind == "tool_result"`
    - `parseRequestMessages("/v1/responses", body含input[]含role=tool元素)` → 该元素 `Kind == "tool_result"`、`Role == "tool"`
    - `parseRequestMessages("/v1/responses/compact", body input为string)` → 输出含 `Role == "user"`、`Kind == "text"` 的元素
    - `parseAssistantContent("/v1/chat/completions", false, 非流式含choices[0].message.tool_calls)` → 输出含 `Kind == "tool_use"` 且 `Text` 形如 `get_weather({"city":"北京"})` 的元素；纯文本元素 `Kind == "text"`
    - `parseAssistantContent("/v1/chat/completions", true, 流式SSE)` → 增量文本正确累积，`Kind == "text"`
    - `parseUsage("/v1/chat/completions", false, 含顶层usage{prompt_tokens:10,completion_tokens:5})` → `(10, 5)`
    - `parseUsage("/v1/chat/completions", true, 末chunk无usage)` → `(0, 0)`
    - `MsgPart` 经 `common.Marshal`/`common.Unmarshal` 往返后与原值相等（JSON 合规）
  - 验证命令：`cd e:/Agent-zone/sili-smart-trace && go test ./service/ -run 'TestParseRequestMessages|TestParseAssistantContent|TestParseUsage|TestMsgPartRoundTrip' -v`
  - 预期输出：所有用例 `--- PASS`，测试命令退出码 0。

  **specs 依据：**
  - 章节范围：§2.4 能力2（协议消息解析）、§2.4 能力3（用量解析）、§2.3 输出定义（MsgPart 结构）、§3.3 兼容性要求（`common.Marshal/Unmarshal` 约束、路径白名单）
  - 业务规则索引：
    - [BR1] §2.4 能力2：OpenAI `role=tool` 标 `Kind=tool_result`，Claude `tool_result` block 标 `Kind=tool_result`，首轮全部 request 侧消息按请求原序
    - [BR2] §2.4 能力2：三协议增量字段位置各不相同，解析器必须按 path 分支
    - [BR3] §2.4 能力2：失败/错误响应同样入库，assistant 侧解析失败记空
    - [BR4] §2.4 能力3：流式未带 `include_usage` 时 `completion_tokens` 记 0
    - [BR5] §3.3 兼容性：消息序列化经 `common.Marshal`/`common.Unmarshal`，禁止直接调用 `encoding/json`

  **依赖：** 无

  **提交信息：** feat(conversation): 新增三协议消息解析与用量解析（由控制器统一提交）

---

- [x] **T3: service 层启发式会话识别**

  **文件：**
  - 修改：`service/conversation.go`
  - 测试：`service/conversation_test.go`（由实现者按 TDD 指导创建）

  **契约：**
  - 函数签名：
    ```go
    package service

    // prefixHash 按 token_id 分桶计算本轮 request 侧内容的前缀指纹。
    // 对组合后的 request 侧消息文本取前 64 字符计算指纹。
    func prefixHash(tokenID int, requestParts []MsgPart) string

    // resolveSessionKey 会话指纹到 sessionKey 的映射，返回是否新建会话。
    // Redis 可用走 Redis 映射（跨实例共享）；不可用退化进程内 map 单实例模式。
    func resolveSessionKey(tokenID int, requestParts []MsgPart) (sessionKey string, isNew bool)

    // redisSetNX 原子抢占（SET NX EX），直连 common.RDB，不新增 common/redis.go 方法。
    func redisSetNX(key, value string, ttl time.Duration) (bool, error)

    // redisExpire 命中即滚动续期 TTL。
    func redisExpire(key string, ttl time.Duration) error
    ```
  - 关键逻辑：
    - 本轮 request 侧内容组合规则：纯文本轮为末条 `role=user` 消息文本；含工具结果轮为全部 `tool_result` 消息文本 + 末条 `role=user` 消息文本（tool_result 在前）；首轮为全部 user 侧消息文本（按请求原序）。组合后取前 64 字符，`prefixHash` 用 `fnv` 或 `sha` 摘要 + `token_id` 分桶（同 token_id 同内容得同指纹）。
    - `resolveSessionKey`：构造缓存键 `conv:session:{token_id}:{prefixHash}`，TTL 30min。先判断 `common.RedisEnabled`：不可用则查进程内 `sync.Map`（未命中则生成新 `sessionKey` 存入，返回 `isNew=true`），并在首例退化时经 `common.SysError` 标注单实例模式（`sync.Once` 保护日志）；Redis 可用时先 `common.RedisGet` 重读，未命中生成新 `sessionKey` 后 `redisSetNX` 原子抢占，胜出者用自己的 sessionKey（`isNew=true`），失败者再 `RedisGet` 重读已存在 sessionKey；命中则 `redisExpire` 滚动续期。Redis 运行期出错（连接失败/超时）时捕获链路不中断，按新会话回退生成 sessionKey，`common.SysError` 记录。sessionKey 用 `common.NewRequestId()` 生成。
    - 键构建用 `strconv.Itoa(tokenID)`。

  **验收锚点：**
  - 核心断言：
    - `prefixHash(1, [{user,text,"你好"}])` 与 `prefixHash(1, 相同内容)` 相等；与 `prefixHash(1, 不同内容)` 不相等；与 `prefixHash(2, 相同内容)` 不相等（token_id 分桶）
    - `resolveSessionKey` 在 `common.RedisEnabled=false`（测试内保存并恢复原值）时：同 tokenID + 同 requestParts 连续两次调用返回相同 sessionKey 且第一次 `isNew=true`、第二次 `isNew=false`；不同内容返回不同 sessionKey
  - 验证命令：`cd e:/Agent-zone/sili-smart-trace && go test ./service/ -run 'TestPrefixHash|TestResolveSessionKey' -v`
  - 预期输出：所有用例 `--- PASS`，测试命令退出码 0。

  **specs 依据：**
  - 章节范围：§2.4 能力4（启发式会话识别）、§3.5 可靠性要求（会话续链、单实例退化、Redis 运行期故障）、§4.3 依赖项（go-redis）
  - 业务规则索引：
    - [BR1] §2.4 能力4：按 `token_id` 分桶 + 内容前缀哈希（前 64 字符），键形如 `conv:session:{token_id}:{prefixHash}`，TTL 30min
    - [BR2] §2.4 能力4：本轮 request 侧增量按内容类型组合（纯文本轮末条 user；工具轮 tool_result 在前 + 末条 user；首轮全部 user 侧消息按原序）
    - [BR3] §2.4 能力4：命中复用 `sessionKey` 并滚动续期（`RedisExpire`）；未命中 `RedisSetNX` 原子抢占，失败者 `RedisGet` 重读
    - [BR4] §3.5 可靠性：`!common.RedisEnabled` 时退化进程内 map，`common.SysError` 标注单实例模式
    - [BR5] §3.5 可靠性：Redis 运行期故障时本轮按新会话回退生成 sessionKey，捕获链路不中断

  **依赖：** T2

  **提交信息：** feat(conversation): 新增启发式会话识别与 Redis 会话缓存（由控制器统一提交）

---

- [x] **T4: service 层会话编排与对话拼接**

  **文件：**
  - 修改：`service/conversation.go`
  - 测试：`service/conversation_test.go`（由实现者按 TDD 指导创建）

  **契约：**
  - 类型与函数签名：
    ```go
    package service

    // ConversationInput 是一次请求/响应的纯值快照，middleware 同步段构造，
    // 经 gopool.Go 传入 RecordConversation。字段来自这一次请求/响应或 context，
    // 不查 logs、不做计费运算。Path/RawRequestBody/RawResponseBody 为解析输入。
    type ConversationInput struct {
        SessionKey        string
        RequestID         string
        CreatedAt         int64
        Messages          []MsgPart // 由 RecordConversation 内部解析后填充
        TurnKind          string
        Truncated         int64
        ModelName         string
        ChannelID         int
        TokenID           int
        TokenName         string
        UserID            int
        Username          string
        Group             string
        IP                string
        UseTime           int64
        IsStream          bool
        UpstreamRequestID string
        PromptTokens      int
        CompletionTokens  int
        // 解析输入（middleware 同步段捕获的原始数据）
        Path            string
        RawRequestBody  []byte
        RawResponseBody []byte
    }

    // RecordConversation 异步编排：解析 → 会话识别 → 组装轮次 → 写库。
    func RecordConversation(input ConversationInput)

    // MergeConversation 按 created_at, request_id 升序逐行 append 每行 messages 列，
    // 得到完整 []MsgPart 序列。
    func MergeConversation(turns []model.ConversationTurn) []MsgPart
    ```
  - 关键逻辑：
    - `RecordConversation`：调用 `parseRequestMessages(input.Path, input.RawRequestBody)` 与 `parseAssistantContent(input.Path, input.IsStream, input.RawResponseBody)` 得 `requestParts`/`assistantParts`；调用 `parseUsage(...)` 得 prompt/completion tokens；调用 `resolveSessionKey(input.TokenID, requestParts)` 得 sessionKey 与 isNew。`TurnKind` 判定：requestParts 或 assistantParts 含 `Kind=tool_use` 或 `Kind=tool_result` 时记 `tool_round`（优先级最高）；否则 `isNew` 时记 `first`；否则记 `normal`。`Messages` 为 `append(requestParts, assistantParts...)`（请求侧在前、响应侧在后，物理时序即真实顺序）。组装 `model.ConversationTurn`（含全部维度字段，`Messages` 经 `common.Marshal` 序列化；`Username` 经 `resolveUsername(input.Username, input.UserID)` 处理：middleware 捕获的 `username` 在 relay 侧恒为空串，`UserID` 有效时回退查 `model.GetUsernameById` 解析真实用户名，查询失败或无效 ID 保留原值、不 panic、不阻塞写库）调 `model.RecordConversationTurn`；写库失败经 `common.SysLog` 记录。首次调用时 `model.EnsureConversationTable()` 已由 middleware 触发（T5），此处不重复建表。
    - `MergeConversation`：对 `turns` 逐行 `common.Unmarshal(turns[i].Messages)` 解码为 `[]MsgPart` 后按序 append；解码失败的行记空并 `common.SysLog` 记录，不中断拼接。

  **验收锚点：**
  - 核心断言：
    - `MergeConversation([{Messages:"[{role:user,kind:text,text:a}]"}, {Messages:"[{role:assistant,kind:text,text:b}]"}])` → 输出 `[]MsgPart` 长度为 2，第 0 个元素 Role=`user`、Text=`a`，第 1 个元素 Role=`assistant`、Text=`b`（按序拼接）
    - `MergeConversation` 含一行非法 JSON `Messages` → 该行记空，其余行正常拼接，不 panic
    - `RecordConversation` 的 TurnKind 判定（将判定逻辑抽为可测私有函数 `turnKindFor(parts []MsgPart, isNew bool)`）：含 `tool_use` 元素 → `"tool_round"`；无工具且 `isNew=true` → `"first"`；无工具且 `isNew=false` → `"normal"`
    - `resolveUsername("explicit_user", 3)` → `"explicit_user"`（显式非空原样返回，不查 DB）；`resolveUsername("", 3)`（DB 存在 id=3 用户名 real_user）→ `"real_user"`（回退解析真实用户名）；`resolveUsername("", 0)` → `""`（无效 ID 保留原值）；`resolveUsername("", 99)`（查无用户）→ `""`（保留空串，不 panic）
  - 验证命令：`cd e:/Agent-zone/sili-smart-trace && go test ./service/ -run 'TestMergeConversation|TestTurnKindFor' -v`
  - 预期输出：所有用例 `--- PASS`，测试命令退出码 0。

  **specs 依据：**
  - 章节范围：§2.4 能力1（service 输入契约 ConversationInput）、§2.4 能力5（调用示例：组装 ConversationTurn 一次写库）、§2.4 能力6（MergeConversation）、§3.2 性能要求（异步写库）、§2.3 输出定义（turn_kind 枚举、消息序列化）
  - 业务规则索引：
    - [BR1] §2.3 输出定义：turn_kind 判定，含工具调用记 `tool_round`（优先于 first/normal），新建会话首轮记 `first`，其余记 `normal`
    - [BR2] §2.4 能力5：`messages` 单列按「请求侧增量在前、响应侧在后」存本轮完整序列，元素带 role/kind
    - [BR3] §2.4 能力5：所有字段来自这一次请求/响应或 context，无查 logs、无计费运算、无二次更新
    - [BR4] §2.4 能力6：轮次顺序由查询侧按 `created_at, request_id` 升序 append，写入路径不维护 `turn_seq`
    - [BR5] §3.2 性能：异步写库整体在 `gopool.Go` 异步 goroutine 内，闭包只捕获纯值

  **依赖：** T1, T2, T3

  **提交信息：** feat(conversation): 新增会话写库编排与对话拼接（由控制器统一提交）

---

- [x] **T5: middleware 层对话捕获**

  **文件：**
  - 创建：`middleware/conversation_log.go`
  - 测试：`middleware/conversation_log_test.go`（由实现者按 TDD 指导创建）

  **契约：**
  - 类型与函数签名：
    ```go
    package middleware

    // ConversationLog 返回挂载在 relay 路由分组上的 gin middleware。
    // 读取 CONVERSATION_LOG_ENABLED：关时直接 c.Next() 零开销；开时先按路径
    // 白名单过滤，命中才继续捕获，随后 gopool.Go 调 service.RecordConversation。
    func ConversationLog() gin.HandlerFunc

    // conversationResponseWriter 照抄 auditResponseWriter 的包装模式。
    type conversationResponseWriter struct {
        gin.ResponseWriter
        body      *bytes.Buffer
        maxSize   int
        truncated int64 // 累计截断字节数
    }

    func (w *conversationResponseWriter) Write(b []byte) (int, error)
    func (w *conversationResponseWriter) WriteString(s string) (int, error)

    // isConversationPath 路径白名单：OpenAI/Claude 精确匹配，Gemini 后缀匹配。
    func isConversationPath(path string) bool
    ```
  - 关键逻辑：
    - `ConversationLog()`：读取 `common.GetEnvOrDefaultBool("CONVERSATION_LOG_ENABLED", false)`（关闭时每次请求直接 `c.Next()`，不读 Body、不包装 writer、不建表）。开启时先 `isConversationPath(c.Request.URL.Path)` 过滤，未命中直接 `c.Next()`。命中后：
      1. 调用 `model.EnsureConversationTable()`（`sync.Once` 内部保护，仅首次建表）。
      2. 请求体：`common.GetBodyStorage(c)` 触发缓存，`storage.Bytes()` 后 `bytes.Clone`（`memoryStorage.Bytes()` 返回内部 slice 引用，BodyStorageCleanup 请求结束即 Close）。
      3. 响应体：构造 `conversationResponseWriter{ResponseWriter: c.Writer, body: bytes.NewBuffer(nil), maxSize: 256 * 1024}` 替换 `c.Writer`，`c.Next()` 后 `bytes.Clone(writer.body.Bytes())`。
      4. 同步段捕获纯值标量：`RequestID` 用 `c.GetString(common.RequestIdKey)`；`ModelName` 用 `common.GetContextKeyString(c, constant.ContextKeyOriginalModel)`；`ChannelID/TokenID/UserID` 分别用 `GetContextKeyInt(c, constant.ContextKeyChannelId/ContextKeyTokenId/ContextKeyUserId)`；`TokenName/Username` 用 `c.GetString("token_name")`/`c.GetString("username")`（`username` 在 relay 侧恒为空串，真实用户名由 service 层按 `UserID` 回退解析，见 T4）；`Group` 用 `GetContextKeyString(c, constant.ContextKeyUsingGroup)`；`IP` 用 `c.ClientIP()`；`IsStream` 用 `GetContextKeyBool(c, constant.ContextKeyIsStream)`；`UpstreamRequestID` 用 `c.GetString(common.UpstreamRequestIdKey)`；`CreatedAt` 用 `common.GetTimestamp()`；`UseTime` 为 `c.Next()` 前后 `time.Since` 取秒。构造 `service.ConversationInput`（含 `Path`、`RawRequestBody`、`RawResponseBody`、`Truncated: writer.truncated`），`gopool.Go(func() { service.RecordConversation(input) })`。
    - `conversationResponseWriter.Write`：仿 `auditResponseWriter.Write`（middleware/audit.go:24）：`body.Len() < maxSize` 时写满剩余空间，超出部分计入 `truncated`；始终透传 `w.ResponseWriter.Write(b)`。`WriteString` 委托 `Write`。响应体超 256KB 时 `truncated` 置为截断字节数（供 `Truncated` 字段落库）。
    - `isConversationPath`：精确匹配 `/v1/chat/completions`、`/v1/completions`、`/v1/responses`、`/v1/responses/compact`、`/v1/messages`；后缀匹配 `:generateContent`、`:streamGenerateContent`。白名单集中在此一处。

  **验收锚点：**
  - 核心断言：
    - `isConversationPath("/v1/chat/completions") == true`、`isConversationPath("/v1/messages") == true`、`isConversationPath("/v1beta/models/gemini-1.5-pro:generateContent") == true`、`isConversationPath("/v1/models/gemini:streamGenerateContent") == true`、`isConversationPath("/v1/embeddings") == false`、`isConversationPath("/v1/audio/transcriptions") == false`
    - `conversationResponseWriter` 截断：maxSize=10 时写入 25 字节，`body` 长度恰为 10，`truncated == 15`
    - `ConversationLog()` 关闭时（`t.Setenv("CONVERSATION_LOG_ENABLED", "")`）：请求命中白名单路径，响应透传且 `c.Writer` 未被替换为 `conversationResponseWriter`
  - 验证命令：`cd e:/Agent-zone/sili-smart-trace && go test ./middleware/ -run 'TestIsConversationPath|TestConversationResponseWriter|TestConversationLogDisabled' -v`
  - 预期输出：所有用例 `--- PASS`，测试命令退出码 0。

  **specs 依据：**
  - 章节范围：§2.4 能力1（对话捕获）、§2.2 输入定义（middleware 挂载点、请求数据来源）、§3.2 性能要求（开关关闭开销、响应 buffer 上限、异步写库）、§3.3 兼容性要求（路径白名单）、§3.4 安全要求（内存安全）
  - 业务规则索引：
    - [BR1] §2.4 能力1：关闭时直接 `c.Next()` 零开销，不包装 writer、不读 Body、不建表
    - [BR2] §2.4 能力1：取值时机必须在 `c.Next()` 后的同步段，body 取出后立即 `bytes.Clone`
    - [BR3] §3.2 性能：响应 buffer 上限 256KB，超限截断并置 `truncated` 标记、记录截断字节数
    - [BR4] §3.3 兼容性：白名单集中一处，OpenAI/Claude 精确路径匹配，Gemini 后缀匹配；不覆盖 `/v1/realtime`、转写/TTS/embedding/image/rerank 等非对话路径
    - [BR5] §3.2 性能：异步写库在 `gopool.Go` 内，闭包只捕获纯值，不阻塞响应

  **依赖：** T4

  **提交信息：** feat(conversation): 新增对话捕获 middleware（由控制器统一提交）

---

- [x] **T6: controller 层管理查询接口与 i18n**

  **文件：**
  - 创建：`controller/conversation.go`
  - 修改：`i18n/keys.go`、`i18n/locales/en.yaml`、`i18n/locales/zh-CN.yaml`、`i18n/locales/zh-TW.yaml`
  - 测试：`controller/conversation_test.go`（由实现者按 TDD 指导创建）

  **契约：**
  - 函数签名：
    ```go
    package controller

    // ListConversations 返回会话列表，仅聚合字段，不含消息内容。
    func ListConversations(c *gin.Context)

    // GetConversation 返回 {session, messages, turns}。session 为聚合元数据；
    // turns 为逐轮元数据（不含每轮消息内容）；messages 为按 created_at, request_id
    // 升序 append 得到的完整 []MsgPart 序列，是详情响应的唯一消息内容来源。
    func GetConversation(c *gin.Context)
    ```
  - i18n keys 新增（`i18n/keys.go`）：
    ```go
    MsgConversationNotFound = "conversation.not_found"
    ```
    三语言 locales yaml 各新增 `conversation.not_found` 键（en：`conversation not found`，zh-CN：`会话不存在`，zh-TW：`會話不存在`）。
  - 关键逻辑：
    - `ListConversations`：解析 `token_name`/`username`/`model_name`（`c.Query`）、`start_timestamp`/`end_timestamp`（`strconv.ParseInt`）。校验时间范围倒置（`start_timestamp > end_timestamp` 且均非 0）时 `common.ApiErrorI18n(c, i18n.MsgInvalidParams)` 返回。分页用 `common.GetPageQuery(c)`（page_size 上限 100）。调 `model.ListConversations(params, pageInfo.GetPage(), pageInfo.GetPageSize())`，`pageInfo.SetTotal(int(total))`、`pageInfo.SetItems(summaries)`，`common.ApiSuccess(c, pageInfo)`。
    - `GetConversation`：`sessionKey := c.Param("session_key")`，空则 `common.ApiErrorI18n(c, i18n.MsgInvalidParams)`。长会话分页单独解析 `p`/`page_size`（不走 `GetPageQuery` 的 100 上限），抽私有 helper `clampDetailPageSize(pageSize int) int`：page_size 超 200 时 clamp 到 200（对齐 `GetPageQuery` 的超上限 clamp 语义），p < 1 时归 1。调 `model.GetConversationTurns(sessionKey)`；无记录（`len(turns) == 0`）时 `common.ApiErrorI18n(c, i18n.MsgConversationNotFound)`。对 turns 按当前页切片（单响应上限 200 轮，超限截断并在响应中置 `truncated` 提示），`service.MergeConversation(pageTurns)` 得完整消息序列。`session` 聚合元数据取首行维度字段 + 首末轮时间（首行/末行 `CreatedAt`）+ `TurnCount`（总轮数）；`turns` 为逐轮元数据 `{id, created_at, request_id, turn_kind, truncated}`，不含每轮消息内容。不回传 `ip`/`channel_id`/`token_id`/`use_time` 等整表维度字段。`common.ApiSuccess(c, gin.H{"session": ..., "turns": ..., "messages": ...})`。
    - 错误返回按项目惯例经 `common.ApiErrorI18n`/`common.ApiError`（HTTP 200 + `success=false`），语义状态码见 specs §2.3 错误码表（404 会话不存在、400 参数非法、500 内部错误）。

  **验收锚点：**
  - 核心断言（controller 逻辑抽可测私有 helper）：
    - `ListConversations` 时间范围校验：`start_timestamp=100`、`end_timestamp=50` → 返回 400（`success=false`），不调 `model.ListConversations`
    - `GetConversation` 无记录：mock `model.GetConversationTurns` 返回空 → 响应 `success=false` 且 message 为 i18n 键对应的翻译值
    - `GetConversation` 分页上限：`page_size=500` → 被 clamp 至 200（抽 `clampDetailPageSize(pageSize int) int` 断言 `clampDetailPageSize(500) == 200`、`clampDetailPageSize(50) == 50`）
  - 验证命令：`cd e:/Agent-zone/sili-smart-trace && go test ./controller/ -run 'TestListConversations|TestGetConversation|TestClampDetailPageSize' -v && go build ./...`
  - 预期输出：所有用例 `--- PASS`；`go build ./...` 无错误输出。

  **specs 依据：**
  - 章节范围：§2.3 输出定义（REST API、会话列表查询参数、会话详情返回结构）、§2.4 能力7（管理员查询 API）、§3.4 安全要求（访问控制、审计）、§2.5 使用示例
  - 业务规则索引：
    - [BR1] §2.4 能力7：查询参数与 logs 对齐（`token_name`/`username`/`model_name`/`start_timestamp`/`end_timestamp`）；列表分页沿用 `common.GetPageQuery`（page_size 上限 100）
    - [BR2] §2.4 能力7：长会话详情分页单独解析，不走 `GetPageQuery` 的 100 上限，`page_size` 允许最大 200；单响应上限 200 轮，超限截断并置 `truncated` 提示
    - [BR3] §2.3 输出定义：`messages` 为当前返回 turns 内按 `created_at, request_id` 升序 append 的完整 `[]MsgPart` 序列，唯一消息内容来源；`turns` 为逐轮元数据，不含每轮消息内容
    - [BR4] §2.4 能力7：不回传整表维度字段（ip/channel_id/token_id/use_time 不进入详情响应）
    - [BR5] §2.4 能力7：session_key 无记录返回 404（项目惯例经 `common.ApiErrorI18n` 表达）；参数非法返回 400；错误信息经 go-i18n 国际化
    - [BR6] §3.4 安全：查询 API 仅管理员，挂管理员路由组（路由注册在 T7）

  **依赖：** T1, T4

  **提交信息：** feat(conversation): 新增会话列表与详情管理查询接口（由控制器统一提交）

---

- [x] **T7: 路由挂载（relay 捕获 + 管理查询）**

  **文件：**
  - 修改：`router/relay-router.go`（第 84-85 行之间、第 194-199 行之间各加一行）
  - 修改：`router/api-router.go`（`logRoute` 定义附近新增 conversation 路由组）

  **契约：**
  - relay 捕获挂载（最小侵入，仅此一处改动文件）：
    ```go
    // router/relay-router.go，httpRouter 创建后、httpRouter.Use(middleware.Distribute()) 之前（第 84-85 行之间）
    httpRouter.Use(middleware.ConversationLog())

    // router/relay-router.go，relayGeminiRouter 创建后、relayGeminiRouter.Use(middleware.Distribute()) 之前（第 194-199 行之间）
    relayGeminiRouter.Use(middleware.ConversationLog())
    ```
  - 管理查询挂载（仿 `logRoute` 写法，router/api-router.go）：
    ```go
    conversationRoute := apiRouter.Group("/conversation")
    conversationRoute.Use(middleware.AdminAuth())
    conversationRoute.GET("/", controller.ListConversations)
    conversationRoute.GET("/:session_key", controller.GetConversation)
    ```
    产生路径 `/api/conversation/` 与 `/api/conversation/:session_key`。
  - 关键逻辑：`httpRouter` 上挂载使 OpenAI 精确路径（`/v1/chat/completions`、`/v1/completions`、`/v1/responses`、`/v1/responses/compact`）与 Claude（`/v1/messages`）及 Gemini `/v1/models/*` 入口被捕获；`relayGeminiRouter` 上挂载使 Gemini `/v1beta/models/*:generateContent`、`:streamGenerateContent` 入口被捕获。两者均注册在 `Distribute()` 之前作为外层，middleware 自身 `c.Next()` 后会读到 `Distribute` 写入的 `channel_id` 等 context 字段。

  **验收锚点：**
  - 核心断言：无（路由挂载无单测），改为编译与挂载确认。
  - 验证命令：
    - `cd e:/Agent-zone/sili-smart-trace && go build ./...`
    - `grep -n "ConversationLog" router/relay-router.go`
    - `grep -n "conversation" router/api-router.go`
  - 预期输出：
    - `go build ./...` 无错误输出，退出码 0；
    - `relay-router.go` 出现两处 `httpRouter.Use(middleware.ConversationLog())` 与 `relayGeminiRouter.Use(middleware.ConversationLog())`，且均在对应 `Distribute()` 挂载之前；
    - `api-router.go` 出现 `conversationRoute.GET("/", controller.ListConversations)` 与 `conversationRoute.GET("/:session_key", controller.GetConversation)`。

  **specs 依据：**
  - 章节范围：§2.2 输入定义（middleware 挂载点）、§2.4 能力7（router/api-router.go 注册调用示例）、§3.1 硬约束（最小侵入，仅 1~2 处挂载）、§4.1 安装方式、§4.2 引入方式
  - 业务规则索引：
    - [BR1] §2.2 middleware 挂载点：`httpRouter` 注册在 `Distribute()` 之前作为外层；`relayGeminiRouter` 同法挂载
    - [BR2] §3.1 硬约束：现有代码改动仅 `router/relay-router.go`（两处 middleware 挂载）与 `router/api-router.go`（查询入口，可选）；relay/channel/ 下所有 handler、`relay_info.go`、`text_quota.go`、`constants.go`、`option.go`、`main.go` 一律不动
    - [BR3] §2.4 能力7：`GET /api/conversation/` 与 `GET /api/conversation/:session_key` 挂在管理员路由组（`middleware.AdminAuth()`），仅管理员可访问
    - [BR4] §3.1 硬约束：`CONVERSATION_LOG_ENABLED` 走环境变量（`common.GetEnvOrDefaultBool`），不进后台 OptionMap

  **依赖：** T5, T6

  **提交信息：** feat(conversation): 挂载对话捕获 middleware 并注册管理查询路由（由控制器统一提交）

---

## 变更日志

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-08-01 | v1 | 初始计划 |
| 2026-08-01 | v2 | 阶段二终审契约回改：T1 assignTurnIds 签名改值切片（查询侧回填）；T2 补 OpenAI Responses `input[]` 解析与断言；T4 补 `resolveUsername` 真实用户名回退与断言；T5 补 username 恒空由 service 回退说明 |
